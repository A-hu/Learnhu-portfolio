<% if current_user.like_portfolios.include?(portfolio_item) %>
  <%= link_to fa_icon('thumbs-up'), like_portfolio_path(portfolio_item), method: :post, remote: true %>
<% else %>
  <%= link_to fa_icon('thumbs-o-up'), like_portfolio_path(portfolio_item), method: :post, remote: true %>
<% end %>
