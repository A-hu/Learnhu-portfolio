<% if current_user.like_topics.include?(@topic) %>
  <%= link_to fa_icon('rss'), like_topic_path(@topic), method: :post, style: 'opacity: 1;', remote: true %>
<% else %>
  <%= link_to fa_icon('rss'), like_topic_path(@topic), method: :post, style: 'opacity: .3;', remote: true %>
<% end %>
